# SMS-Spam-Detection
SMS spam detection uses machine learning (ML) and deep learning (DL) to automatically classify text messages as "ham" (legitimate) or "spam" (fraudulent/unwanted), filtering them before they re Common techniques include Natural Language Processing (NLP) for text preprocessing, algorithms like Naive Bayes, SVM, and trained the model for Prediction .
